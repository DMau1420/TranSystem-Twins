{
	"name": "frontend",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"lint": "eslint .",
		"preview": "vite preview"
	},
	"lint-staged": {
		"frontend/**/*.{js,jsx,ts,tsx}": [
			"npx --prefix frontend eslint --fix",
			"prettier --write"
		]
	}
}